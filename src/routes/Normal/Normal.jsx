import { GameHeader } from "../../components/GameHeader/GameHeader"
import StyledNormal from "./StyledNormal"

export const Normal = () => {
    return (
        <StyledNormal>
            <GameHeader/>
        </StyledNormal>
    )
}
